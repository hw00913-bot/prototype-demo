# 页面标注维护规则

本文件只约束项目已有的页面标注系统。项目目标、业务规则、阶段推进和验收仍分别以 `CLAUDE.md`、`memory/` 和 `tools/loop_run.py` 为准。

## 职责边界

- 标注运行时固定为 `annotations/annotation-runtime.js` 和 `annotations/annotation.css`。
- 正式标注数据固定为 `annotations/annotations.js`。
- S7 只在业务源码中补语义锚点，不生成标注数据。
- S9 只生成 `memory/annotation-prompt.md`、`memory/annotation-coverage.md` 和 `docs/interaction.html`，不自动写正式标注。
- 只有 PM 明确要求回写时，才依据本轮提示词一次性写入正式标注。
- 不修改业务逻辑、业务样式、业务文案、路由或 Mock 数据来迁就标注。

## 硬性禁止

1. 不得读取、合并、续写底座或旧项目标注。
2. 不得把浏览器缓存、旧导出 JSON 或历史提示词当作本轮正式数据。
3. 不得自动新增业务标注、示例标注或占位标注。
4. 不得提供页面内“新增标注”入口。
5. 不得创建文件写回服务、`saveEndpoint` 或 `enableFileWriteback: true`。
6. 不得手写阶段 pass、修改 `config/workflow.json` 或修改 `memory/stage-log.md`。
7. 资料不足时报告缺口，不把“待确认、TODO、TBD”写入正式标注。

## 源码锚点合同

锚点必须挂在最小、可解释、可唯一定位的业务元素上，并同时声明：

```html
<button
  data-anno="task-create"
  data-anno-page="task-list"
  data-anno-label="创建任务"
  data-anno-kind="action"
  data-anno-fields="FLD-001,FLD-002">
  创建任务
</button>
```

规则：

- `data-anno` 在整个项目业务源码中全局唯一。
- `data-anno-page` 必须等于该页面的 page key。
- `data-anno-label` 是业务功能名称，必须与标注 `sections.functionName` 一致。
- `data-anno-kind` 只能使用 `action`、`field`、`status`、`region`、`table`、`dialog`、`chart`、`filter`。
- 涉及字段时，`data-anno-fields` 必须列出真实存在的 `FLD-*`；无字段时省略。
- 按钮、输入框、标签、列标题、弹窗标题优先直接挂锚点。
- 禁止把动作或字段锚点挂到 `html`、`body`、`main`、页面根容器、大卡片、整块筛选区或包含多个控件的父容器。
- S9 只能使用提示词锚点清单里的 selector；缺锚点时回到 S7，不在 S9 临时创建。

## 正式数据格式

`annotations/annotations.js` 必须是严格 JSON 对象赋值，不能包含注释、函数、动态表达式或尾随逗号：

```js
window.AnnotationData = {
  "task-list": [
    {
      "id": "1",
      "page": "task-list",
      "target": "[data-anno='task-create']",
      "title": "创建任务",
      "sourceRefs": ["SRC-001"],
      "fieldRefs": ["FLD-001", "FLD-002"],
      "sections": {
        "functionName": "创建任务",
        "functionDesc": "说明该功能解决的问题。",
        "permissionScope": "说明可使用角色或本期权限假设。",
        "dataSource": "说明来源并对应 SRC 编号。",
        "valueLogic": "说明取值、计算或映射逻辑。",
        "fieldDesc": "FLD-001 字段名｜定义：业务含义｜逻辑：取值逻辑｜格式：展示格式｜异常：空值规则\nFLD-002 字段名｜定义：业务含义｜逻辑：映射逻辑｜格式：展示格式｜异常：异常规则",
        "interactionDesc": "说明点击、输入、切换和反馈。",
        "judgeRule": "说明显示、可用或状态判断条件。",
        "exceptionRule": "说明空状态、失败和边界处理。",
        "otherDesc": "说明原型限制或本期无其它说明。"
      }
    }
  ]
};
```

数据规则：

- ID 必须是从字符串 `"1"` 到 `"N"` 的全局连续数字，跨页面不得重新编号。
- 每条标注必须有 `sourceRefs`；字段相关标注的 `fieldRefs` 必须与锚点字段合同完全一致。
- `sections` 的 10 个字段都必须有明确业务解释。
- `fieldDesc` 每个 `FLD-*` 独立一行，每行都包含“定义、逻辑、格式、异常”。
- 不得只罗列字段名、接口字段名或编号，也不得把多个字段压在同一行。
- `desc` 如需兼容旧展示，只能由 `sections` 派生，不作为主编辑或事实来源。

## 运行时不变量

- 页面只有一个左上角显示/隐藏按钮；无正式标注时不显示标注点。
- 标注点使用红色主色并贴近目标元素，selector 必须唯一匹配。
- 已有标注可查看、关闭、拖动、缩放和分字段编辑。
- 页面编辑只保存到浏览器缓存，并提供复制/导出；浏览器缓存不自动写回文件。
- SPA 页面变化只刷新标注点，不重复创建按钮、弹窗或事件监听。
- 截图、导出和临时验证文件不得留在交付目录。

## 手工回写与验证

1. 回写前确认 `annotations/annotations.js` 是本轮空骨架；非空时先确认来源，禁止静默合并。
2. 只使用本轮 `memory/annotation-prompt.md` 的页面、锚点、`SRC-*` 和 `FLD-*`。
3. 一次性写入本轮完整 `window.AnnotationData`，再检查 JS 语法、页面加载、selector 唯一性和多字段换行。
4. 项目已进入终态时运行：

```bash
python3 tools/loop_run.py approve-annotations .
```

只有该命令重新通过 final 并刷新终态快照后，才能说明标注回写完成。
