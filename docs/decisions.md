# 项目决策记录

## D-019 2026-09-05 时长调减允许归零

- 依据SRC-020，用户明确要求“改成可以归0”。原要求只对充值输入及单次调整量要求正整数，没有要求调减结果必须保留1天；撤销该实现附加限制。
- 时长调减上限=当前配置总天数，结果允许0、不得为负；调整量仍须大于0整数且原因必填。
- 保留时长调整对原到期日加减天数的方式；归零强制已过期，其他结果按更新后的到期时间刷新状态。归零不清空余额、不动既有冻结、消耗或结算记录。
- 本轮现有正式标注来源可确认：SRC-019生成且已通过approve-annotations；本次仅维护同一规则的文字及SRC-020引用，不重建或继承历史标注，不改变ID和锚点。

## D-018 2026-09-05 四项评审修正与当前标注回写

- 来源：SRC-019，沿用SRC-014、SRC-016、SRC-018。
- 渝发及海粤试用演示权益采用trial_package；渝发试用流水同步0元参考价，已手改天数和分钟快照不重置。默认30天/500分钟仅用于新开通。
- 其他管理员演示账号绑定当前渝发，只读展示已授权当前租户；缺有效上下文时提示不可用。创建、充值、调整及使用明细权限不扩大。
- 用户本次明确指出标注未显示，触发空骨架完整回写；先通过S8、生成本轮提示词，再写正式数据、验证并运行approve-annotations；不继承旧标注或缓存。
- 计算说明以实际统一分钟池实现为准：时长调整作用于配置总天数及到期日，充值包不延长原服务期。旧14节保留为已停用历史原文。
- 全量说明增量维护，租户管理菜单/页面演示差异继续注明，不新增路由隔离。

## D-017 2026-09-05 保留原全量说明并注明权限演示差异

- 依据：SRC-018，用户确认本次可不改租户管理权限实现，但须在说明中注明；原全量说明及统一权限章节必须保留。
- 核对：原 docs/功能说明文档.md 与 HTML 均仍存在且与版本库基线一致；上一轮只是将右上角入口误换成迭代子文档，并未删除原文件。
- 决策：恢复原右上角全量文档入口，原Markdown增量更新v3.2并使用原渲染器生成HTML；原全部章节、版本记录和非本期内容保留。迭代说明v1.7提供全量及权限章节链接，覆盖D-016仅链接迭代子文档的入口安排。
- 权限唯一维护出处：全量说明五.7。租户管理整个菜单、页面和管理操作为超管专属；当前原型尚未完整实现菜单和页面隔离。本次只补说明，不修改JS、业务样式、Mock或正式标注。现有创建、充值、调整和使用情况校验保留。
- 后续正式标注若重建，必须引用该权限章节并区分目标规则和当前演示限制；本次不重建标注数据。

## D-016 2026-09-05 非超管验证与交付说明统一

- 依据：SRC-017，用户要求提供非超管账号验证首页和使用情况，并同步版本、规则及标注说明。
- 决策：复用现有 tenantUser（渝兴租户用户），以白名单 demoAccount 链接进入，无真实密码或生产权限。多租户验证使用现有 multiTenantUser。
- 影响：两处说明入口统一到 docs/interaction.html；v1.6 汇总当前规则与验证路径；更新当前标注准备材料，正式标注须明确确认后重建，不读取或续写旧标注。

## D-015 2026-09-05 管理操作归属超级管理员

- 依据 SRC-016，创建租户、充值和手工调增调减统一由 super_admin 操作，覆盖原平台管理员/充值管理员分工。
- 入口和提交使用同一角色判断；切换账号关闭旧操作窗口，等待中的充值取消；非超管直调不产生记录或改账。
- 新建租户更新人及新充值/调整操作人取当前登录账号，不固定写充值管理员；默认演示超级管理员，其他管理员保留作无权限对照。
- 超管继续不提供使用情况及租户明细，普通/多租户用户仅可查看当前租户用量。本次不扩大到无关编辑、删除或任务执行权限。
- 原始分析图保留溯源，交付来源提示和当前功能说明声明最新角色口径。

## D-014 2026-09-05 取消历史兼容功能

- 依据：SRC-015，用户确认旧功能未发布生产，无历史兼容或迁移需求。
- 删除历史兼容页签、展示及两条专用演示记录；保留内部充值、手工调整流水及冻结消耗。
- 关闭 OI-002/004/005，R-015 撤出范围；覆盖此前历史只读假设，原始需求图及旧步骤保留溯源。

## D-013 2026-09-05 试用套餐正名与租户类型互斥

- 依据：SRC-014，用户纠正套餐名称并明确按租户类型显示配置；“使用租户”按语境理解为“试用租户”。覆盖 D-012 的试用可同时选择三种充值类型。
- `trial_package` 正式展示名改为“试用套餐”；试用租户仅提供此充值类型。商用租户仅提供现有标准版年包、话费充值包配置，不新增未确认的商品。
- 抽屉入口、表单类型、规则说明与打开/提交校验共用类型白名单，非法类型或不匹配租户均拒绝写账；提交时重新读取当前租户标记，防止表单打开后标记变化。
- 试用默认0元/30天/500分钟与手工编辑规则不变；商用默认规则不变。手工调增调减和冻结结算保留，历史流水及历史权益不因切换标记被改写。未到期重复开通继续拦截，试用提示使用手工调整而非购买话费包。

## D-012 2026-09-05 原图复用与独立使用套餐

- 依据：SRC-013 用户最新两项明确修正，覆盖 D-011 中“试用沿用同套餐”的假设；不修改冻结启动规划。
- 业务流程图与时序交互图直接复用需求分析输出的完整 HTML 图集。交付副本只补导航、来源说明、无视觉影响的合同属性，以及时序明细表的局部滚动容器；不重新绘制、不删减原场景、不改 SVG 几何及原文，冻结需求源保持原样。后续评审新增规则在说明文档另行注明，不冒充原图已有内容。
- 试用租户新增第三种充值类型“使用套餐”（trial_package），默认本次价格 0 元、实际使用时长 30 天、实际入账 500 分钟；三个值均可手工编辑，沿用金额、正整数与偏离原因校验。标准版年包与话费充值包继续保留。
- 使用套餐仅对试用租户可选；开通保留 trial 标记，写入独立产品类型、内部流水、服务期和统一分钟池，首页同步显示“使用套餐”。冻结/结算/手工调增调减规则不变；沿用已有未到期不重复开通限制，不引入续费叠加规则。

## D-011 2026-09-05 原型评审修正

- 依据：SRC-012，用户明确要求处理五项原型问题，授权当前交付版本的修正。
- 充值管理删除“冻结与结算”说明展示区；冻结、计量、结算和释放行为继续沿用既有规则。
- 试用租户显示“开通使用套餐”入口，沿用此前确认的标准版与充值包规则；开通不改变商用/试用标记，未开通租户先开通再购买充值包。
- 套餐价格按“本次总价”手工编辑，支持 0 元及最多两位小数。默认总价随包数计算；已手改的总价在改数量时保留。价格偏离默认值沿用必填原因规则，内部流水记录实际总价及默认总价。
- 使用情况入口放入右上角账户下拉菜单，普通租户可见，超级管理员隐藏；进入后菜单自动关闭。
- 首页属于本次修正范围，读取当前登录租户套餐、有效期、统一可用分钟和呼叫状态；不再由旧金额账本折算大小模型余额。平台账号显示管理入口。
- 当前文档为冻结启动规划之后的修正依据，不回改启动快照。

## D-001 静态原型架构

- 决策：使用 HTML、CSS 和原生 JavaScript，不引入构建工具。
- 依据：SRC-011。
- 影响：所有页面、mock、交互和交付图均可通过本地 HTTP 服务运行。

## D-002 基于现有智能外呼中台底座迭代

- 决策：沿用现有导航、企业级后台视觉和租户/任务基础结构；不重写无关板块。
- 依据：SRC-008、SRC-011。
- 影响：S5 先读取真实结构，S6 再决定文件落点。

## D-003 统一分钟池替代大小模型分池展示

- 决策：标准版、充值包和手工分钟调整进入租户级统一分钟池，大/小模型共同冻结、释放和消耗。
- 依据：SRC-002、SRC-004。
- 影响：所有余额、冻结、消耗和使用情况字段统一以分钟展示。

## D-004 新充值取消外部充值单依赖

- 决策：标准版、充值包和手工调整不读取或关联 D智链单号，系统生成内部流水。
- 依据：SRC-002、SRC-010。
- 影响：删除新充值流程中的“输入单号—读取—关联”前置交互，保留历史数据兼容。

## D-005 商用/试用仅作标记

- 决策：租户仅有商用、试用两种展示标记，不触发差异化计费。
- 依据：SRC-002、SRC-004。
- 影响：租户管理和充值管理新增标签，规则与额度逻辑不分叉。

## D-006 充值值可编辑且偏离须说明

- 决策：使用时长按天、入账分钟均可编辑且必须为大于 0 的整数；偏离默认值时原因必填，无额外审批。
- 依据：SRC-002、SRC-004、SRC-005。
- 影响：充值表单必须展示默认值、实际值、原因和结果预览，并覆盖校验失败状态。

## D-007 手工调整覆盖双方向和双对象

- 决策：调增/调减均可作用于使用时长或可用分钟；调减不得改冻结记录或减为负数。
- 依据：SRC-002、SRC-004。
- 影响：需要四种组合、边界提示、前后值预览、并发冲突和调整流水。

## D-008 当前租户两级使用情况

- 决策：普通租户用户从右上角查看当前登录租户；一级按日、二级按任务；超级管理员无入口。
- 依据：SRC-002、SRC-005。
- 影响：需要角色切换 mock、当前租户隔离、空态/错误态和合计一致性验证。

## D-009 未决历史与续费事项采用保守展示

- 决策：未到期续费不演示；历史外部单号和金额调整只读保留；50% 话术不实现自动判定。
- 依据：SRC-011、OI-001~OI-004。
- 影响：原型不得暗示已确定生产迁移或计费方案。

## D-010 标注人工触发

- 决策：S9 只生成标注提示词、交互说明和覆盖清单，不自动写入标注数据。
- 依据：SRC-011。
- 影响：完成前验证源码锚点和提示词覆盖，但保留 `annotations/annotations.js` 为人工回写入口。
