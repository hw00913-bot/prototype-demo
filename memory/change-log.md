# 项目变更记录

## 2026-09-05

- Step 19 收尾：正式48条标注在全部对应状态验证通过，首页十维弹窗/8字段换行及桌面窄屏正常；运行时仅标最上层弹窗，不把背景红点叠入，隐藏目标与文档/图集不显示标注，账户菜单开闭自动同步。全量说明v3.3保留原23小节，其中18个非本期小节逐字未变；迭代v1.8、计算v2.0和当前字段/覆盖说明同步。48编号全局连续、来源和字段均按当前合同校验，无旧标注继承。

- Step 19 标注回写：本轮48条正式数据已从空骨架完整写入，含十维说明及逐字段解释。浏览器复核发现同URL资源仍取空骨架缓存，更新正式数据版本；同时修复运行时隐藏元素红点和账户下拉属性变化刷新，避免隐藏入口出现离位标注。返回当前步骤重新回归，不改变任何业务权限。

- Step 19：修正渝发和海粤试用种子权益类型及渝发试用流水，保留已编辑天数、分钟；其他管理员绑定渝发并展示只读租户首页，创建/充值/调整/用量权限不扩大。修复SPA标注页面判定和全局账户锚点加载；当前空骨架按SRC-019反馈在验证与本轮提示词准备后回写。计算说明更新统一分钟池，原14节保留为折叠历史；全量和迭代说明增量同步版本及字段事实。

- Step 18 收尾：原23小节、15条旧版本保留，18个非本期小节逐字对照一致；原Markdown渲染同步、浏览器原入口/全量链接、权限差异、既有用量、五视图及final通过。依文档维护边界补入SRC-018/R-032标注来源说明，不读取、继承或回写正式红点数据。

- Step 18：原全量Markdown/HTML核对未被删除；恢复右上角全量文档入口，并在迭代说明显式提供全量与权限章节链接。全量说明增量更新v3.2，保留全部原章节、历史版本和非本期正文，补充值现行摘要并标明旧方案失效；权限唯一出处仍为五.7，说明超管专属和当前原型未完整隔离的差异。迭代说明更新v1.7。未修改业务权限JS、业务样式、Mock或正式标注。

- Step 17 收尾：当前48个锚点重新扫描并生成v1.6标注说明、覆盖清单，显式对账17来源、51字段和31项验收（R-015取消）；未读取旧标注或历史提示词，正式红点未覆盖。浏览器完成非超管直达、两级用量对账、权限对照、文档统一入口及桌面/窄屏五视图验证。

- Step 17：新增现有非超管演示账号白名单直达链接，不创建生产凭证；右上角功能说明和交付导航统一到本次迭代文档并原页切换。说明更新 v1.6，补账号/验证步骤/权限对照，汇总套餐、首页、标题和移除功能规则；S9 同步当前标注生成说明与覆盖清单，正式红点未静默覆盖。

- Step 16 收尾：功能说明更新 v1.5，明确超管创建/充值/调整和用量拒绝边界；按当前48个锚点重新准备权限标注提示词及覆盖清单，覆盖16来源、51字段、30项验收（R-015 已取消）。不改正式标注数据，final及文档检查通过。

- Step 16 回归补充：手工调整成功后同步刷新租户列表，避免抽屉关闭后列表仍显示调整前余额；不改变记账规则。

- Step 16：按 SRC-016/D-015 将创建租户、充值、手工调增调减限制为超级管理员，入口与提交双重校验；切换账号关闭旧窗口并取消未落账充值。新建更新人、充值和调整操作人取当前账号；默认超管，其他管理员保留无权限对照。首页管理提示与原图交付层来源提示同步，业务原图不变。

- Step 15 收尾：新建/编辑表单在四种宽度下共八组检查通过，单选/保存与五视图回归正常；首次复测缓存已通过新版本入口消除。现有 48 个锚点及说明内容未变，final 预检通过。

- Step 15：修复新建/编辑租户中商用/试用标签换行；标签栏统一 110px、不换行，控件间隔 12px，窄屏选项组可折行但单项完整。仅修改表单样式和 CSS 缓存版本；业务逻辑、字段、源码锚点、说明和标注内容不变。

- Step 14 收尾：说明更新 v1.4，取消兼容展示与迁移假设；当前 48 个源码锚点重新生成提示词及覆盖清单，包含 15 来源、51 字段和 29 项验收（其中 R-015 已取消）。正式标注和冻结原图不变；语法、差异、浏览器和 final 检查通过。

- Step 14：删除历史兼容页签、专用渲染和样式及两条演示记录，兼容页签无效调用不改变当前面板；更新资源版本和关联系统说明。当前内部充值、手工调整及冻结消耗保留。SRC-015/D-014 关闭 OI-002/004/005，R-015 撤出范围。

- Step 13：按 SRC-014 / D-013 正名“试用套餐”，试用与商用共用同一类型白名单控制抽屉入口、单选、打开和提交。试用仅试用套餐；商用仅标准版与话费包，补双向不匹配拦截及租户标记变化后的提交复核；已开通试用提示手工调整。首页、新流水名称同步，未知历史套餐显示“服务套餐”；保留原始历史记录、冻结结算和手工调整。图集只修正交付层来源提示中的笔误，原图不变。
- Step 13 收尾：功能说明更新v1.3，补租户类型互斥及提交校验；49个当前源码锚点重新生成提示词和覆盖清单，覆盖28项验收、51字段与14来源，正式标注数据及冻结启动快照不变。
- Step 12：按 SRC-013 / D-012，将交付图替换为需求分析完整图集的原样复用版本（各 7 场景），只增加交付导航、溯源说明和机器属性，原始需求文件不变；新增 trial_package 使用套餐，试用租户独立第三类型，默认 0 元/30 天/500 分钟并支持手改、校验和内部写账，首页沿用产品映射自动显示真实套餐。更新字段、规则和验收，保留冻结/结算与四组合手工调整。
- Step 12 收尾：时序图源消息表的窄屏溢出只通过交付层局部滚动修复，原 SVG 仍逐字一致；功能说明更新为 v1.2，明确原图分析版本与最新评审规则边界。重新扫描49个当前锚点并生成提示词/覆盖清单（27项验收、51字段、13来源）；人工标注数据及冻结启动快照不变。验证预检不识别空错误数组文本，日志改为等价的“console 0 errors”，未修改验证脚本或检查结论。
- Step 11：响应 SRC-012 五项评审反馈，删除充值抽屉“冻结与结算”说明区及专属样式；试用租户增加“开通使用套餐”入口及未开通演示账号；本次套餐总价可编辑并同步默认值、偏离原因校验和内部流水；使用情况移入账户下拉；首页重写为当前登录租户套餐、服务期、统一可用分钟和呼叫状态，切换账号同步刷新。新增 FLD-050/051，D-011 记录最新规则；修改文件见 STEP-11。
- 评审收尾：同步 `docs/interaction.html` 的五章功能说明和版本记录，以及业务流程、时序交互中的价格校验与账户菜单入口文案；按当前 48 个锚点、25 项验收、51 个字段和 12 份来源重新生成标注提示词与覆盖清单。未修改人工标注数据或已确认的启动计划。

## 2026-09-04

- 创建项目基础框架。
- Step 01：在 `mock/data.js` 增加充值迭代统一事实源 `MockRechargeIteration`，包含商用/试用租户、服务权益、统一分钟池、标准版与话费充值包、内部充值/调整流水、只读历史记录、角色与当前租户上下文、按日及任务级用量、冻结结算台账和演示状态；旧外部充值单继续仅作兼容数据。
- Step 02：升级 `js/pages/sys-tenant.js` 与 `assets/css/app.css`：租户列表改为展示商用/试用、服务状态、有效期、统一可用分钟和冻结分钟；新建/编辑弹窗可维护标记并明确“仅展示”；充值抽屉增加租户 ID/名称/标记上下文；补齐本轮唯一 `data-anno` 锚点。为保证本步交互可验证，最小修复了编辑弹窗丢失编辑态及历史有效期变量未定义两个既有问题。
- Step 03：将租户页新入口切换为充值迭代抽屉：以服务状态、有效期、可用/冻结/累计消耗组成统一分钟池概览；分栏展示内部充值流水、手工调整流水和只读历史兼容数据；新操作区彻底移除外部单号输入、读取与关联；增加正常、无历史、加载失败及重试状态。旧外部流程代码保留为不可达兼容实现。
- Step 04：实现标准版年包与话费充值包统一表单：标准版默认 365 天/10,000 分钟，充值包按包数联动 ¥1,000/包与 3,500 分钟/包；使用天数、入账分钟均可编辑，偏离默认值须填写原因；提供生效/失效/分钟变化预览、套餐服务与话术规则、正整数和有效期校验、写账失败重试。成功后原子更新服务权益/统一分钟池并生成不含外部单号的内部流水。
- Step 05：实现手工调整表单，覆盖调增/调减 × 使用时长/可用分钟四种组合，动态切换单位、当前值、最大调减范围和前后值预览；所有调整原因必填，支持正整数、非负和合法时长边界校验；分钟调减不修改冻结分钟；账户版本冲突时刷新版本并要求二次确认。成功后更新目标值并写入完整内部调整流水，历史金额记录继续只读隔离。
- Step 06：新增 `js/pages/usage.js` 当前租户使用情况页，并在 `index.html` 右上角接入角色控制入口和演示登录上下文；`js/app.js` 暴露登录态 getter/setter，`js/nav.js` 增加使用情况路由，`config/nav.json` 记录 header/tenant_user/current 范围。页面按当前登录租户展示有效期、唯一可用分钟和按日消耗，覆盖 loaded、empty、expired、error/retry 及无权限状态；超级管理员隐藏入口，跨租户请求返回权限拦截且不泄露明细。
- Step 07：在 `js/pages/usage.js` 增加按日到任务的二级明细状态，展示当前租户、日期、一级日汇总、任务 ID/名称/模型/接通与未接通数量/消耗分钟及当日合计；返回后恢复一级列表状态。任务合计与一级值不一致时明确报数据异常且不静默修正，并覆盖任务明细空态、加载失败/重试和跨租户拦截；移动端表格在局部容器内横向滚动。
- Step 08：将现有冻结释放入口接入租户统一分钟池，增加余额足额预占、已接通向上取整且最低 1 分钟、未接通 0 分钟、完成结算退回余额、终止全量释放及重复回调幂等保护；充值管理增加当前预占、计费规则和最近结果可视区，新流水继续无外部单号，历史外部单号/金额仅读。
- Step 09：将 `flowcharts/business-process.html` 从空壳更新为 ready 状态的本地 HTML 业务流程图；使用 7 个固定栅格泳道、28 个唯一节点和 21 条唯一边，分别呈现充值/手工调整、任务冻结/结算、当前租户两级用量查询，并将校验失败、写账失败、版本冲突、余额不足和权限拦截收敛为可读异常回流。
- Step 10：生成 ready 状态的本地 HTML 时序交互图，以 5 个参与者和 23 条自上而下消息覆盖充值校验/写账、手工调整版本冲突、任务冻结/结算/幂等、当前租户两级用量与权限拦截；关联系统页保持 empty 合同，明确 D智链读取/关联为 `not_required`，历史外部单号/金额仅读，本轮不伪造外部系统卡片。
