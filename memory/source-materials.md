# 资料记录

> 将产品经理输入、外部文档、截图、历史项目、口述要求、LLM WIKI 输出和补充资料整理为可引用记录。需求、验收、验证、交互说明和标注提示词应引用这些来源编号，而不是只依赖对话上下文。

## 使用规则

- 每条资料使用稳定编号：`SRC-001`、`SRC-002`。
- 没有外部资料时，也必须记录 `SRC-000 | No external source`，说明本轮只来自产品经理口述。
- 如使用独立 LLM WIKI，先把 query 输出作为 `SRC-*` 写入本表，并在 Key Points 中保留 `wiki_ref`、`raw_ref`、`status`、`coverage` 等元数据。
- 标注提示词和手动回写标注中的 `sourceRefs` 应优先引用这里的编号。

## Sources

| Source ID | Type | Title | Received At | Key Points | Used In |
| --- | --- | --- | --- | --- | --- |
| SRC-001 | PM requirement | 删除任务详情红框内容 | 2026-09-03 | 用户明确要求“去掉图中的内容”。结合截图，删除确认记录、当前呼叫轮次、是否最后一次计划呼叫。 | 启动规划、业务规则、验收、标注 |
| SRC-002 | Screenshot | 大众通信任务详情红框截图 | 2026-09-03 | 红框覆盖“大众通信重呼追溯”区内三行：确认记录、当前呼叫轮次、是否最后一次计划呼叫；红框不覆盖人工配置确认。 | 页面范围、视觉验收、标注 |
| SRC-003 | Existing prototype | 智能外呼中台静态原型底座 | 2026-09-03 | 沿用既有任务详情结构、重呼模式分支、视觉样式和场景配置门禁。 | 实现、回归验证、交互说明 |

## LLM WIKI

- 本轮不调用 LLM WIKI；不存在 wiki_ref、raw_ref、partial 或待核对输入。
